# Chat update releases

This repository hosts update files for Chat, a personal home voice
assistant that runs on its owner's own computer. The app checks
`latest.json` every few hours and installs new versions by itself.

- `latest.json` - points at the newest build
- `releases/` - versioned zip builds
